package com.luminos.woosh.enums;

/**
 * 
 * @author Ben
 */
public enum ShareMethod {

	CLONE,							// the user gets their own copy of the card (this is the default)
	TRANSFER;						// the user gets the card and the original is deleted
	
}
